Automatic dependency detection for a whole D script/app (a dlink default rule)
A nice way to pass all compiler flags to the system
Makefile backend for dcompile, cppcompile, etc.
Tup backend
Function for obtaining list of dub targets (all "source" files from all packages):
  should return a simple Target[] with dependencies for each D file baked in
Static libraries
CMake-like rules for rerunning itself
Fix errors when trying to write out source files, test rerunning reggae
Compiler flags and the like for convenience functions such as dcompile, cppcompile, etc
Make global configuration available
